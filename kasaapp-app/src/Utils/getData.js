// On importe la data de data.json
import data from "../Data/data.json";

// Puis on en fait une export fonction qui retourne la data de data.json
export default function getData() {
    return data;
}